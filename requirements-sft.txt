# Extra deps for caption-conditioned SFT only (the frozen text encoder in bnet/captions.py).
# The core train/eval path does NOT need these -- install only on the box that runs SFT.
transformers>=4.40
